[24] Alert - 객체 매핑
    객체 배열을 만들어 키 : 값(객체배열) 으로 되어있는 객체 배열을 만들었다.
    여기서 성공,에러, 경고 키를 작성하고 각 키값에 매핑되는 객체배열{}을 만들었다. 

    그리고 해당 객체배열map[type]을 cfg 변수로 받아 
    p태그에 속성, 내용등을 배열의 내부 값을 통해 렌더링을 하였다. 
    이를 통해 객체 매핑을 학습하였다.

[25] Rating - 배열 메서드
   [...Array(5)].map((_, i) => 는 좀 복잡해보이는 문법 구조인것 같다. 조금 더 자세히 파헤쳐서 알아볼 필요가 있다. 

[26] Tag - 배열 + map
    이번 시간엔 tags라는 props스로 배열을 받아 map으로 펼쳐 배열 목록을 렌더링 하는 것을 해봤다. 
    .map이 계속해서 나오는거 보니 집중 공략을 해보도록 하자.

[27] MUI 라이브러리
    my-app 폴더 위치에서 cmd 로 다음 명령어를 통해 UI 관련 패키지를 설치한다.
    npm install @mui/material @emotion/react @emotion/styled

    해당 라이브러리를 사용하는 컴포넌트인 SubButton.jsx 생성
    import {Button, ...} MUI 라이브러리에서 지원하는 컴포넌트들은 다음과 같다.

    ** 여긴 표로 구현되면 좋을것 같다.
    분류	컴포넌트	뭐 하는 애
    입력(Inputs)	TextField Checkbox Switch Slider Select Radio	글자 입력·체크·토글·슬라이더
    표시(Data)	Typography Avatar Chip Badge Tooltip Divider	글씨·프로필·태그·알림점·툴팁
    피드백	Alert Dialog Snackbar CircularProgress	알림·팝업·토스트·로딩 스피너
    표면(Surfaces)	Card Accordion Paper AppBar	카드·접이식·상단바
    레이아웃	Box Stack Container Grid	배치용 상자·정렬

    여기서 난 
    import {Button, TextField, Typography, Alert, Card, Box} from '@mui/material'
    을 사용해서 웹에 렌더링되는 모습을 확인해봤다.

[28] 컴포넌트 위치
컴포넌트를 어디에 위치하여 끼울지 설정하는 법에 대해 학습했는데 
각 컴포넌트들은 Header, Menu, Content, Footer 를만들었는데 

Header에서 로그인 로그아웃 상태 표시와 함께 버튼에서도 로그인/로그아웃하기 로 상태에 따라 바뀌게 하고싶었는데 잘 안되었다 (추후 집에서 추가 학습하여 내용을 추가하도록한다.)

[29] useState 카운터 
useState 처럼 import에서 {} 내부에 불러오는 것들을 훅이라고 부른다고 하는거 같다. 
이번 예제엔 버튼을 눌렀을 때 버튼 내부의 값이 +1 되는 실습을 진행하였다. 
const [count, setCount] = useState(0) 으로 사용하는데 useState(0)은 초기값을 정하는건 알겠는데 count가 선언되었는데 실습에선 정작 count는 표시하는데만 사용하고 setCount(c => c + 1) 처럼 count를 증가하는 것이었다.. 이게 어떻게 동작하는지에 대해 설명이 필요할것 같다.

[30] ~ [31] 29번에서 배운 useState 카운터 응용으로 값의 +-와 리셋 까지 하는 기능을 해보는 실습을 하였다.
    아래 3개의 버튼 처럼 SetCount 전체적인 사용틀에 대해 조금 익히게 되었다.
   <button onClick={() => setCount(c => c + 1)}>V03 증가</button>
    <button onClick={() => setCount(c => c - 1)}>V03 감소</button>
    <button onClick={() => setCount(0)}>V03 초기화</button>

[32] input 태그의 value 가 값이 변경될때마다 바로 글자가 보이는 웹을 만들어봤다 count 대신 name을 사용하여 onChange이벤트로 값이 변하는것을 해봤다. 
강사님이 추가 시간으로 CSS 도한번 꾸며가며 추가 학습을 해보라 하셨는데 코드 이해하는데 시간이 전부 가서 못했다. (집에서 추가 학습한다는 얘기는 블로그 정리 내용에 넣지말기!)CSS부분은 집에서 추가적으로 학습해보도록하자 

[33] 부모(app.jsx)로 부터 props(name)값을 전달 받고 또 useState를 활용하여 버튼 클릭시 값이 증가하는 실습을 해봤다.

[34] useEffect 
웹페이지가 딱 1번 렌더링 될떄만 실행하는 훅으로 의존성 배열을 사용한다. 사용형태는 useEffect (() => { ... }, []) 형태로 사용
실습으로 console.log를 찍어보았는데 console을 확인해보면 2번 찍혀있는 것을 알 수 있다. 
이건 main.jsx에서 StrictMode 컴포넌트에 의해 웹이 크래쉬 나지 않기 위해 테스트용으로 한번 더 실행해주는 것이라고 한다.

[35] useEffect 심화
화면을 그린 직후 실행할 코드 - 타이머,API 호출 등 화면 밖 동작을 연결하는데 주로 사용
두번째 칸 [] "처음 1번만 등록"
 타이머 함수인 setInterval, clearInterval로 타이머 설정 및 타이머 정리 작업을 하고 또 useState를 통해 시간 값을 저장 및 변경하는? 것을 해보았다.

 [36] useEffect 심화 2
 useEffect 는 실행할때 한번만 렌더링 된다했는데 이번 실습에선 useEffect(() => {...}, [])에서 []내부에 만약 변화할 값이 들어가게 된다면 
 해당 값이 변화할때마다 useEffect가 재실행하여 한번 더 렌더링 된다.
 이를 확인하기 위해 count 변수와 버튼을 만들고 count 증가할때마다 console.log를 찍어 확인해보았다.

---
지금까지 실습을 하면서 useState를 사용하는것에서 
const [count, setCount] = useState(0)형식으로 사용했는데 
타이머나 문자열을 변환을 하는데 사용하려고 보면 위처럼 뿐만아니라 다른 다양한 함수명으로도 선언해서 사용하는 것을 보았다.. 
이건 useState 훅에서 이미 만들어진 함수인건지와 아니면 임의로 여기서 선언 및 생성하는 함수라서 이름은 아무렇게나 상관없지만 기능에 대해 알아보기 위해서 이름들을 바꾸는건지 궁금하다.

 [37] 렌더링 될때마다 매번 실행하도록 하는 useEffect 방법
useEffect (() => {...}, [])에서 [] 이 부분이 아예 없다면 해당 useEffect는 무한 실행되어 프로그램에 큰 부하가 걸린다.

[38] 데이터(API)를 가져와 웹에 렌더링해보았다. 
User들의 데이터들을 얻을 수 있는 API였는데 해당 API 사이트를 확인해보면 User의 데이터에 대해 볼수 있는데 여기서 이름 ID, 회사 이름 등 다양한 정보들을 얻어올 수 있었어 
여기서 그냥 JS 에서 데이터를 읽어오는 await? 그것과 꽤 차이가 난다는것을 알았는데 추가 정리가 필요해 보인다.

[39] Weather API 를 useEffect로 읽어와서 웹에 날씨 표기를 하는 예제를 해보았다 
이전 실습과 마찬가지로 데이터를 읽어왔는데 흠.. 아직 fetch('API주소').then((res) => res.json()).then((data) => {setTemp(data.current_weather.temperature); setIsLoading(false)}.catch((error) => {
    //에러시 실행할 부분
}))
의 기본적인 구조가 있는데 눈에 잘 익지가 않다보니 나중에 또 응용하라고하면 쉽지 않을것 같다. 기본적으로 저건 동기방식인거같은데 비동기방식은 교재에추가적으로 기재되어있는 비동기 방식의 데이터 읽어오는 방법이 있는데 이번 실습 파일(Weather.jsx)에선 작성하지 못했다. 교재를 참고하고 추가 실습 파일을 만들어서 확인하고 비교하며 정리가 필요할 것 같다.